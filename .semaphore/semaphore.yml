version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Tests
    task:
      jobs:
        - name: test
          commands:
            - sem-version python 3.7
            - checkout
            - python -m pip install --upgrade pip
            - pip install pytest
            - make test
