version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Tests
    task:
      jobs:
        - name: test
          commands:
            - whoami
            - checkout
            - sudo apt-get install python3-venv
            - make test
